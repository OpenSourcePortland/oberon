<%= form_for(@character) do |f| %>
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @character.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="field">
    <%= f.label :species %><br />
    <%= f.text_field :species %>
  </div>
  <% if false %> <!--MARK REVIEW: waiting for mark to help us fix single form for multiple models -->
  <%= f.fields_for :ship do |builder|%>

   <div class="field">
     <%= builder.label :name %><br />
     <%= builder.text_field :name %>
   </div>
   
   <% end %>
   <% end %>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
