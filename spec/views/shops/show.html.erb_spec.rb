require 'spec_helper'

describe "shops/show" do
  before(:each) do
    # @shop = assign(:shop, stub_model(Shop,
    #   :location_id => 1,
    #   :name => "Name"
    # ))
    
    @shop = Fabricate(:shop)
  end


end
